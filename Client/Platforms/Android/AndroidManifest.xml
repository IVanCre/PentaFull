<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
	<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />	<!-- Разрешение для Android 13+ на показ уведомлений -->
	<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" /><!-- Разрешение для отображения уведомления на весь экран (как звонок) -->
	<uses-permission android:name="android.permission.WAKE_LOCK" />	<!-- Чтобы будить экран на старых версиях Android -->
	
	<application
		android:name="Client.Platforms.Android.MainApplication"
		android:enabled="true"
		android:allowBackup="true" 
		android:icon="@mipmap/appicon" 
		android:supportsRtl="true">

		<meta-data
			android:name="com.google.firebase.messaging.default_notification_icon"
			android:resource="@drawable/hands" />
		
		<service android:name="com.companyname.penta.MyFirebaseMessagingService"
         android:exported="false">
			<intent-filter>
				<action android:name="com.google.firebase.MESSAGING_EVENT" />
			</intent-filter>
		</service>

		<provider
			android:name="androidx.core.content.FileProvider"
			android:authorities="${applicationId}.fileprovider"
			android:exported="false"
			android:grantUriPermissions="true">
			<meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
		</provider>
	</application>
</manifest>